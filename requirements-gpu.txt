# GPU Accelerated Dependencies for Face-motion-gpu
# Optimized for NVIDIA RTX 3060 (12 GB VRAM) + Multi-core CPU (64 threads)

# Core Deep Learning & ONNX Runtime with CUDA
torch>=2.1.0
torchvision>=0.16.0
torchaudio>=2.1.0
onnx>=1.14.0
onnxruntime-gpu>=1.16.0

# Vision Processing & Models (use headless for Linux servers)
opencv-python-headless>=4.8.0
numpy>=1.24.0,<2.0.0
emotiefflib>=0.1.0
pillow>=10.0.0
timm>=0.9.0

# Audio Processing & Prosody
soundfile>=0.12.0
librosa>=0.10.0
scipy>=1.10.0
transformers>=4.30.0
accelerate>=0.20.0
sherpa-onnx>=1.10.0

# Optional Prosody (Gold Standard for Pitch / Jitter / Shimmer)
praat-parselmouth>=0.4.3

# Utilities
tqdm>=4.66.0
tabulate>=0.9.0
